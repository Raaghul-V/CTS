create database cts;

use cts;

CREATE TABLE user(firstName VARCHAR(50), lastName VARCHAR(50), gender VARCHAR(50),email VARCHAR(50),contactNumber VARCHAR(50),customerId VARCHAR(50),password VARCHAR(50));

select * from user;